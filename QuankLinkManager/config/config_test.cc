#include <gtest/gtest.h>

namespace {
// This test case only for test the build system.
// Remove this test case after any other test cases are added.
TEST(ConfigTest, Test) { EXPECT_EQ(1, 1); }
}  // namespace
